# 电商商品价格自动化采集与对比工具设计文档

## 1. 整体架构设计
该工具采用分层架构设计，确保模块间低耦合，易于扩展和维护。分为数据采集层、数据处理层、应用层和展现层。

- **展现层 (前端)**：采用 HTML + Vue.js + Element Plus 构建网页界面。使用 Apache ECharts 实现价格趋势、性价比气泡图的可视化展示。
- **应用层 (后端)**：采用 Python FastAPI 构建轻量级 Web 服务，对外提供 RESTful API，同时处理网页和命令行的入口逻辑。
- **数据处理层**：使用 Pandas 进行数据清洗（去空、去重）、类型转换、价格排序以及性价比模型计算。
- **数据采集层**：使用 Playwright (异步模式) 模拟真实浏览器行为，绕过基础的反爬虫机制，抓取主流电商平台（京东、淘宝等）的搜索结果页面的商品数据。考虑到平台风控策略（特别是淘宝/拼多多强制登录验证），本工具设计了**真实采集**与**模拟演示(Mock)**两种模式，保证在任何环境下工具的可演示性。

## 2. 数据获取核心逻辑
核心采集模块 `scraper.py` 负责调度。
1. **关键词搜索与页面加载**：利用 Playwright 打开目标电商搜索链接（例如京东的 `https://search.jd.com/Search?keyword={keyword}`），等待商品列表渲染完成。
2. **DOM 树解析与数据提取**：通过 CSS 选择器或 XPath 提取商品标题、价格、销量、店铺评分、商品链接和平台标识。
3. **防封禁策略**：
   - 随机 User-Agent
   - 随机滚动页面触发懒加载
   - 设置合理的请求间隔 (Sleep)
4. **统一数据结构**：抓取到的原始数据统一转换为以下格式：
   ```json
   {
       "id": "jd_1000123",
       "platform": "京东",
       "title": "某品牌手机",
       "price": 4999.00,
       "sales": 10000,
       "rating": 4.8,
       "url": "https://item.jd.com/1000123.html"
   }
   ```

## 3. 数据清洗与性价比计算逻辑
采集完毕后，将数据交由 `Pandas` DataFrame 处理：
1. **数据清洗与去重**：剔除价格或标题为空的异常数据，根据商品 ID 或 URL 进行去重。
2. **排序与横向对比**：将不同平台相同关键词的商品汇总，按价格从低到高进行全局排序。
3. **性价比评分模型**：
   - 使用极差标准化将 `price`, `sales`, `rating` 缩放至 0-1 区间。
   - 综合得分 `Score = W_rating * norm(rating) + W_sales * norm(sales) - W_price * norm(price)` （示例权重，实际可根据配置调整）。
   - 根据得分在结果中打上“性价比推荐”标注。

## 4. 数据可视化方案
在 Web 演示端中，不仅提供数据表格展示，还提供图表分析：
1. **价格区间分布图（柱状图）**：展示不同价格段的商品数量分布，帮助用户了解市场价格中枢。
2. **价格-销量-评分气泡图（散点图）**：X 轴为价格，Y 轴为销量，气泡大小代表评分/性价比。能够直观地通过散点群找到“价格低、销量高”的高性价比商品区域。
3. **平台均价对比（饼图/柱状图）**：横向对比京东、淘宝等平台在该商品上的平均价格和最低价格。

## 5. 项目结构
- `scraper.py`: 核心命令行抓取脚本及采集器类封装。
- `processor.py`: 数据清洗、排序及性价比评分逻辑。
- `app.py`: FastAPI Web 服务端点。
- `templates/index.html`: Web 演示界面。
- `requirements.txt`: 项目依赖。
